{
  "name": "jev-computer-use",
  "version": "0.1.0",
  "description": "Read-only Jev (TypeSafe System One) UI-candidate selection CLI for Pi",
  "type": "module",
  "bin": {
    "jev-cu": "bin/jev-cu.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/"
  },
  "license": "MIT",
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  }
}
